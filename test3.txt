fn malloc(size: u64) -> *i8;

struct Foo {
    x: i32;
    y: i32;
};

fn test(foo: *Foo) {
    let x = (*foo).y;
    let y: i16 = 10;
}

fn add(x: i32, y: i32) -> i32 {
    return x + y;
}

fn main(argc: i32, argv: **i8) -> i32 {
    let x = argc == argc * (5 + 5);
    printf_ln("%d", x);
    return 0;
}
