struct Vec2 {
    x: f32;
    y: f32;
};

impl Vec2 {
    fn new(x: f32, y: f32) -> Vec2 {
        let mut self: Vec2;
        self.x = x;
        self.y = y;
        return self;
    }
}

fn main(argc: i32, argv: **i8) -> i32 {
    let v = Vec2::new(1, 2);
    return 0;
}
