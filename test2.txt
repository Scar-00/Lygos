fn malloc(size: u64) -> *i8;

struct IVec2 {
    x: i32;
    y: i32;
};

struct Vec2 {
    x: f64;
    y: f64;
};

impl IVec2 {
    fn new(x: i32, y: i32) -> IVec2 {
        let mut self: IVec2;
        self.x = x;
        self.y = y;
        return self;
    }

    fn ptr() -> *IVec2 {
        let mut self = (*IVec2)malloc((u64)8);
        self[0] = 0;
        return self;
    }
}

impl Vec2 {
    fn new(x: f64, y: f64) -> Vec2 {
        let mut self: Vec2;
        self.x = x;
        self.y = y;
        printf_ln("%f | %f", self.x, self.y);
        return self;
    }
}

fn cast(x: *i32) {
    let y = (*i8)x;
}

fn main(argc: i32, argv: **i8) -> i32 {
    let x = (f32)10;
    let y = 10;
    return 0;
}
