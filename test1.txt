fn malloc(size: i32) -> *i32;
fn print_deref(ptr: *i32);

struct Foo {
    x: i32;
};

fn make_foo() -> Foo {
    let mut x: Foo;
    x.x = 200;
}

fn test(i: *i32) {
    print_deref(i);
}

fn main() -> i32 {
    let foo = make_foo();
    test(&foo.x);
    return 0;
}
