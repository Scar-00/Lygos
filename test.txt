fn malloc(size: i32) -> *i32;
fn print_deref(ptr: *i32);

fn print_arr(arr: **i8, length: i32, iter: i32) {
    if iter < length {
        printf_ln("%d -> %s", iter, arr[iter]);
        print_arr(arr, length, iter + 1);
    }else {

    }
}

struct Foo {
    x: i32;
    y: i32;
};

fn foo_init(x: i32, y: i32) -> Foo {
    let mut foo: Foo;
    foo.x = x;
    foo.y = y;
    return foo;
}

fn main(argc: i32, argv: **i8) -> i32 {
    let mut arr = malloc(4 * 4);
    arr[0] = 10;
    arr[1] = 20;
    arr[2] = 30;
    arr[3] = 30;
    if arr[0] < 11 {
        printf_ln("Less then 11");
    }else {
        printf_ln("More then 11");
    }
    return 0;
}
