fn print_int(i: i32) -> i32;

struct Foo {
    x: i32;
    y: i32;
};

struct Human {
    age: i32;
    height: i32;
    foo: Foo;
};

fn test(i: i32) -> i32 {
    let mut foo: Human;
    foo.foo.x = 10;
    return 0;
}

fn main() -> i32 {
    test(0);
    return 0;
}
